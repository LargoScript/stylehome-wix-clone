# Стратегія міграції Style Homes на TypeScript

## 📋 Огляд проекту

### Поточна структура
```
stylehome_new/
├── css/
│   └── style.css (2385 рядків)
├── js/
│   ├── libs/
│   │   ├── anime.min.js (бібліотека анімацій)
│   │   ├── aos.css (AOS стилі)
│   │   └── aos.js (AOS бібліотека)
│   └── main.js (961 рядок - основний код)
├── img/ (медіа файли)
├── video/ (відео файли)
├── index.html
└── README.md
```

### Залежності
- **AOS (Animate On Scroll)** - анімації при прокрутці
- **Anime.js** - складні анімації та ефекти
- **Vanilla JavaScript** - без фреймворків

---

## 🎯 Мета міграції

1. **Типізація коду** - додати типи для всіх функцій та змінних
2. **Покращення підтримки** - легше знаходити помилки на етапі компіляції
3. **Краща IDE підтримка** - автодоповнення, рефакторинг
4. **Масштабованість** - легше додавати новий функціонал
5. **Документація** - типи служать як документація

---

## 📦 Технологічний стек

### Рекомендовані інструменти

#### 1. **TypeScript**
- Версія: `^5.9.3` (або новіша)
- Мета: типізація JavaScript коду

#### 2. **Vite** (опціонально)
- Версія: `^6.0.0`
- Мета: швидка збірка та dev server
- Альтернатива: можна залишити статичний HTML з компіляцією TS

#### 3. **Type Definitions для бібліотек**
- `@types/aos` - типи для AOS (якщо доступні)
- Типи для Anime.js (можливо потрібно створити власні)

---

## 🏗️ Структура проекту після міграції

### Варіант 1: З Vite (рекомендовано)
```
stylehome_new/
├── src/
│   ├── main.ts                    # Головний файл (заміна main.js)
│   ├── types/
│   │   ├── dom.d.ts               # DOM типи та розширення
│   │   ├── anime.d.ts             # Типи для Anime.js
│   │   └── aos.d.ts               # Типи для AOS
│   ├── modules/
│   │   ├── header.ts              # Header функціонал
│   │   ├── navigation.ts           # Навігація та smooth scroll
│   │   ├── animations.ts           # Анімації (AOS, Anime.js)
│   │   ├── carousel.ts             # Каруселі проектів
│   │   ├── testimonials.ts        # Карусель відгуків
│   │   ├── faq.ts                  # FAQ accordion
│   │   └── form.ts                 # Форма (textarea auto-resize)
│   └── utils/
│       ├── dom.ts                  # DOM утиліти
│       └── scroll.ts               # Scroll утиліти
├── public/
│   ├── img/                        # Медіа файли
│   ├── video/                      # Відео файли
│   └── js/libs/                    # Бібліотеки (AOS, Anime.js)
├── dist/                           # Зібраний проект (генерується)
├── css/
│   └── style.css                   # Стилі (без змін)
├── index.html                      # HTML (оновлений для TS)
├── tsconfig.json                   # Конфігурація TypeScript
├── vite.config.ts                  # Конфігурація Vite
├── package.json                    # Залежності
└── README.md
```

### Варіант 2: Без Vite (простий)
```
stylehome_new/
├── src/
│   ├── main.ts
│   ├── types/
│   └── modules/
├── js/
│   └── libs/                       # Бібліотеки
├── css/
├── img/
├── video/
├── index.html
├── tsconfig.json
├── package.json
└── README.md
```

---

## 📝 Аналіз поточного коду

### Основні функції в main.js:

1. **initHeaderScroll()** - ефект скролу для header
2. **initServicesDarkening()** - затемнення services секції
3. **initAutoBurgerMenu()** - автоматичне показ/приховування burger меню
4. **initAnimations()** - ініціалізація AOS та Anime.js
5. **initSmoothScroll()** - плавна прокрутка навігації
6. **initActiveNavLink()** - оновлення активного посилання при скролі
7. **Carousel functionality** - каруселі проектів з infinite loop
8. **Testimonials carousel** - карусель відгуків
9. **FAQ accordion** - акордеон для FAQ
10. **Auto-resize textarea** - автоматична зміна висоти textarea
11. **Mobile navigation** - мобільне меню (burger)

### Глобальні змінні:
- `checkElementsCollision` - функція перевірки колізій
- `closeMenuFunction` - функція закриття меню

---

## 🔧 План міграції

### Етап 1: Підготовка (1-2 години)

1. **Створити структуру проекту**
   ```bash
   mkdir -p src/{types,modules,utils}
   mkdir -p public/{img,video,js/libs}
   ```

2. **Встановити залежності**
   ```bash
   npm init -y
   npm install -D typescript vite @types/node
   npm install -D @types/aos  # якщо доступні
   ```

3. **Створити конфігураційні файли**
   - `tsconfig.json`
   - `vite.config.ts` (якщо використовуємо Vite)
   - `package.json` з скриптами

### Етап 2: Створення типів (2-3 години)

1. **DOM типи та розширення** (`src/types/dom.d.ts`)
   - Типи для DOM елементів
   - Розширення для HTMLElement
   - Типи для dataset

2. **Типи для бібліотек** (`src/types/anime.d.ts`, `src/types/aos.d.ts`)
   - Типи для Anime.js API
   - Типи для AOS API
   - Глобальні декларації

3. **Типи для проекту** (`src/types/index.ts`)
   - Інтерфейси для каруселей
   - Типи для навігації
   - Типи для форм

### Етап 3: Модулізація коду (4-6 годин)

1. **DOM утиліти** (`src/utils/dom.ts`)
   - Безпечні querySelector з типами
   - Утиліти для роботи з DOM

2. **Header модуль** (`src/modules/header.ts`)
   - `initHeaderScroll()`
   - Типізація header елементів

3. **Navigation модуль** (`src/modules/navigation.ts`)
   - `initSmoothScroll()`
   - `initActiveNavLink()`
   - `initAutoBurgerMenu()`
   - Типи для навігації

4. **Animations модуль** (`src/modules/animations.ts`)
   - `initAnimations()`
   - Типи для анімацій

5. **Carousel модуль** (`src/modules/carousel.ts`)
   - Логіка каруселей проектів
   - Типи для каруселей

6. **Testimonials модуль** (`src/modules/testimonials.ts`)
   - Карусель відгуків
   - Типи для testimonials

7. **FAQ модуль** (`src/modules/faq.ts`)
   - FAQ accordion
   - Типи для FAQ

8. **Form модуль** (`src/modules/form.ts`)
   - Auto-resize textarea
   - Типи для форм

### Етап 4: Головний файл (1-2 години)

1. **main.ts** (`src/main.ts`)
   - Імпорти всіх модулів
   - Ініціалізація при DOMContentLoaded
   - Типізація всіх обробників

### Етап 5: Оновлення HTML (30 хвилин)

1. **index.html**
   - Оновити посилання на скрипти
   - Додати module type для скриптів
   - Оновити шляхи до бібліотек

### Етап 6: Тестування (2-3 години)

1. Перевірити всі функції
2. Виправити помилки типізації
3. Перевірити на різних браузерах
4. Перевірити responsive дизайн

### Етап 7: Оптимізація (1-2 години)

1. Мінімізація bundle
2. Tree shaking
3. Оптимізація імпортів

---

## 📚 Типи та інтерфейси

### DOM типи

```typescript
// src/types/dom.d.ts

interface HTMLElementWithDataset extends HTMLElement {
  dataset: {
    index?: string;
    bound?: string;
    aos?: string;
    aosDelay?: string;
    aosDuration?: string;
  };
}

interface CarouselTrack extends HTMLElementWithDataset {
  style: CSSStyleDeclaration & {
    transform: string;
    transition: string;
  };
}
```

### Типи для навігації

```typescript
// src/types/navigation.ts

export interface NavigationConfig {
  headerHeight: number;
  scrollOffset: number;
  smoothScrollDelay: number;
}

export type CloseMenuFunction = () => void;
export type CheckCollisionFunction = () => void;
```

### Типи для каруселей

```typescript
// src/types/carousel.ts

export interface CarouselConfig {
  infinite: boolean;
  transitionDuration: number;
  swipeThreshold: number;
}

export interface CarouselState {
  currentIndex: number;
  totalSlides: number;
  isTransitioning: boolean;
}
```

### Типи для анімацій

```typescript
// src/types/animations.ts

export interface AOSConfig {
  duration: number;
  easing: string;
  once: boolean;
  offset: number;
  delay: number;
  anchorPlacement: string;
}

export interface AnimeConfig {
  targets: string | HTMLElement | HTMLElement[];
  opacity?: number[];
  translateX?: number[];
  translateY?: number[];
  scale?: number[];
  duration: number;
  delay: number | number[];
  easing: string;
}
```

---

## ⚙️ Конфігурація TypeScript

### tsconfig.json

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": false,
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true,
    "outDir": "./dist",
    "rootDir": "./src",
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"],
      "@types/*": ["src/types/*"],
      "@modules/*": ["src/modules/*"],
      "@utils/*": ["src/utils/*"]
    }
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
```

---

## 📦 Конфігурація Vite

### vite.config.ts

```typescript
import { defineConfig } from 'vite';
import { resolve } from 'path';

export default defineConfig({
  server: {
    port: 8000,
    strictPort: true,
    open: true
  },
  publicDir: 'public',
  build: {
    outDir: 'dist',
    assetsDir: 'assets',
    sourcemap: true,
    rollupOptions: {
      input: {
        main: resolve(__dirname, 'index.html')
      }
    }
  },
  resolve: {
    alias: {
      '@': resolve(__dirname, 'src'),
      '@types': resolve(__dirname, 'src/types'),
      '@modules': resolve(__dirname, 'src/modules'),
      '@utils': resolve(__dirname, 'src/utils')
    }
  }
});
```

---

## 📋 package.json

```json
{
  "name": "stylehome-new",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^6.0.0",
    "@types/node": "^20.0.0"
  }
}
```

---

## 🔄 Покрокова міграція

### Крок 1: Створення базових типів

1. Створити `src/types/dom.d.ts`
2. Створити `src/types/anime.d.ts`
3. Створити `src/types/aos.d.ts`

### Крок 2: Модулізація по функціоналу

1. Почати з простіших модулів (header, form)
2. Потім складніші (navigation, carousel)
3. Останнім - animations (залежить від бібліотек)

### Крок 3: Інтеграція

1. Створити `src/main.ts`
2. Імпортувати всі модулі
3. Ініціалізувати при DOMContentLoaded

### Крок 4: Оновлення HTML

1. Замінити `<script src="js/main.js">` на `<script type="module" src="/src/main.ts">`
2. Оновити шляхи до бібліотек

---

## ⚠️ Потенційні проблеми та рішення

### Проблема 1: Типи для Anime.js та AOS

**Рішення:**
- Створити власні `.d.ts` файли
- Використовувати `declare` для глобальних змінних
- Додати `// @ts-ignore` тільки якщо необхідно

### Проблема 2: DOM елементи можуть бути null

**Рішення:**
- Використовувати type guards
- Створити утиліти для безпечного querySelector
- Використовувати non-null assertion (`!`) тільки коли впевнені

### Проблема 3: Dataset типизація

**Рішення:**
- Створити інтерфейси для dataset
- Використовувати type assertions
- Розширити HTMLElement

### Проблема 4: Глобальні змінні

**Рішення:**
- Створити модуль для глобального стану
- Використовувати класи або об'єкти замість глобальних змінних
- Експортувати функції замість зберігання в глобальних змінних

---

## ✅ Чеклист міграції

### Підготовка
- [ ] Створити структуру папок
- [ ] Встановити залежності
- [ ] Створити конфігураційні файли
- [ ] Створити бекап поточного проекту

### Типи
- [ ] Створити DOM типи
- [ ] Створити типи для Anime.js
- [ ] Створити типи для AOS
- [ ] Створити типи для проекту

### Модулі
- [ ] Модуль header.ts
- [ ] Модуль navigation.ts
- [ ] Модуль animations.ts
- [ ] Модуль carousel.ts
- [ ] Модуль testimonials.ts
- [ ] Модуль faq.ts
- [ ] Модуль form.ts
- [ ] Утиліти (dom.ts, scroll.ts)

### Інтеграція
- [ ] Створити main.ts
- [ ] Оновити index.html
- [ ] Перевірити всі імпорти

### Тестування
- [ ] Перевірити header scroll
- [ ] Перевірити навігацію
- [ ] Перевірити каруселі
- [ ] Перевірити анімації
- [ ] Перевірити мобільне меню
- [ ] Перевірити форму
- [ ] Перевірити FAQ
- [ ] Перевірити responsive

### Оптимізація
- [ ] Мінімізація bundle
- [ ] Tree shaking
- [ ] Оптимізація імпортів

---

## 📊 Оцінка часу

- **Підготовка**: 1-2 години
- **Типи**: 2-3 години
- **Модулізація**: 4-6 годин
- **Інтеграція**: 1-2 години
- **Тестування**: 2-3 години
- **Оптимізація**: 1-2 години

**Загалом: 11-18 годин** (1.5-2.5 робочих дні)

---

## 🎯 Переваги після міграції

1. **Типобезпека** - помилки виявляються на етапі компіляції
2. **Автодоповнення** - IDE краще розуміє код
3. **Рефакторинг** - безпечніші зміни коду
4. **Документація** - типи служать як документація
5. **Масштабованість** - легше додавати новий функціонал
6. **Підтримка** - легше знаходити та виправляти помилки

---

## 📝 Наступні кроки

1. **Після міграції:**
   - Додати ESLint з TypeScript підтримкою
   - Додати Prettier для форматування
   - Налаштувати pre-commit hooks
   - Додати unit тести (опціонально)

2. **Можливі покращення:**
   - Використати класи замість функцій
   - Додати state management (якщо потрібно)
   - Розбити на ще більше модулів
   - Додати error handling

---

## 🔗 Корисні ресурси

- [TypeScript Handbook](https://www.typescriptlang.org/docs/handbook/intro.html)
- [Vite Documentation](https://vitejs.dev/)
- [TypeScript DOM Types](https://github.com/microsoft/TypeScript/blob/main/lib/lib.dom.d.ts)
- [Anime.js Documentation](https://animejs.com/)
- [AOS Documentation](https://michalsnik.github.io/aos/)

---

**Дата створення:** 2025-01-17  
**Версія:** 1.0  
**Статус:** Готово до імплементації
